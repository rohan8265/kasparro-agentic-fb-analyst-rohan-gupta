# Release v1.0

This repository draft is ready for v1.0 release.

## What to include in the GitHub release
- Tag: `v1.0`
- Title: `v1.0 — Kasparro Agentic FB Analyst (rohan-gupta)`
- Release notes: Brief description + pointers to `report.md`, `insights.json`, `creatives.json`.

## Create tag locally and push
```bash
git tag -a v1.0 -m "v1.0 — initial submission"
git push origin v1.0
```
